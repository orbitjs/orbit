/**
 Core methods and classes for the Orbit Common ("OC") library.

 OC contains a base abstract class, `Source`, which should be extended to define
 a common set of compatible sources.

 @module orbit-common
 @main orbit-common
 */

const OC = {};

export default OC;
